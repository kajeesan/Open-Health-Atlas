# Copy to an external root-owned environment file such as
# /etc/hermes/insights.env. Never place a populated database in the source tree.
HERMES_DATA_DIR=/var/lib/hermes
HEALTH_DB=/var/lib/hermes/health.db
HERMES_TIMEZONE=UTC
